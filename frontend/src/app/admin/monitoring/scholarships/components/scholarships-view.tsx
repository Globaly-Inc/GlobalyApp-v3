import { AdminNotAvailableView } from "../../../components/admin-not-available-view";

export function ScholarshipsView() {
  return (
    <AdminNotAvailableView
      title="Scholarships"
      description="Manage scholarship listings, featured placement, and publish status."
      wave="G"
      waveScope="Wave G builds scholarships (G1: publish, public directory, moderation)."
    />
  );
}
