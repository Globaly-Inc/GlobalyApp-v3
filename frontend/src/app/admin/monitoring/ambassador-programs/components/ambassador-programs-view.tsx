import { AdminNotAvailableView } from "../../../components/admin-not-available-view";

export function AmbassadorProgramsView() {
  return (
    <AdminNotAvailableView
      title="Ambassadors"
      description="Ambassador programs overview across all businesses."
      wave="G"
      waveScope="Wave G builds ambassador operations (G4: inquiries, threads, earnings, payouts)."
    />
  );
}
