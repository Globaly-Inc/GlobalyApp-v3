// Pass-through, so every feature under src/app has the same shape (frontend/AGENTS.md).
export default function ServicesLayout({ children }: Readonly<{ children: React.ReactNode }>) {
  return children;
}
