node_modules/
dist/
.env
*.log

# Test coverage output
coverage/

# A symlink named node_modules is a FILE, so the "node_modules/" pattern above
# (directory-only, because of the trailing slash) does not match it. One got
# committed that way: a self-referential symlink that destroyed the real tree.
node_modules
